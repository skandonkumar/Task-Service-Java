spring.application.name=task-service-java

# Server Configuration
server.port=8080

# Cassandra Configuration
spring.cassandra.contact-points=${CASSANDRA_HOST:cassandra-service}
spring.cassandra.port=${CASSANDRA_PORT:9042}
spring.cassandra.keyspace-name=task_keyspace
spring.cassandra.local-datacenter=datacenter1
spring.cassandra.schema-action=create_if_not_exists
spring.cassandra.request.timeout=10s

# Redis Configuration
spring.data.redis.host=${REDIS_HOST:redis-service}
spring.data.redis.port=${REDIS_PORT:6379}

# Cache Configuration
spring.cache.type=redis
spring.cache.redis.time-to-live=600000

# Logging
logging.level.com.polyglot.task=DEBUG
logging.level.org.springframework.data.cassandra=DEBUG
